# Job Orchestration

# Building Dockerfiles

- orchestration-service:
  `docker build -f orchestrator-service/Dockerfile . -t orchestration-service`
- worker-service: `docker build -f worker-service/Dockerfile . -t worker-service`
